def run(num):
    w = x = y = z = 0
    i = 0
    input = list(map(int, num))
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 1)
    x = x + 10
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 13
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 1)
    x = x + 13
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 10
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 1)
    x = x + 13
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 26)
    x = x + -11
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 1
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 1)
    x = x + 11
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 9
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 26)
    x = x + -4
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 3
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 1)
    x = x + 12
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 5
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 1)
    x = x + 12
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 1
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 1)
    x = x + 15
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 0
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 26)
    x = x + -2
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 13
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 26)
    x = x + -5
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 7
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 26)
    x = x + -11
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 15
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 26)
    x = x + -13
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 12
    y = y * x
    z = z + y
    w = input[i]
    i += 1
    x = x * 0
    x = x + z
    x = x % 26
    z = int(z / 26)
    x = x + -10
    x = 1 if x == w else 0
    x = 1 if x == 0 else 0
    y = y * 0
    y = y + 25
    y = y * x
    y = y + 1
    z = z * y
    y = y * 0
    y = y + w
    y = y + 8
    y = y * x
    z = z + y
    return z

def main():
    print(run("14753111169114"))
    
    z = -1
    num = int("9" * 14) + 1
    while(z != 0):
        num -= 1
        numStr = str(num)
        
        if numStr.__contains__('0'):    
            continue
        else:
            z = run(numStr)
            print(z)
    
    print(num)

if __name__ == '__main__':
    main()
